
#toc-paginated.toc
  .btn-group-vertical.toc-list
    each chapter in locals.toc
      a(id="#{chapter.contents.slug}").toc-h1.btn.btn-menu(href="/dist/#{chapter.contents.url}")
        b #{chapter.contents.number} &mdash; #{chapter.contents.title}
        each subchapter in chapter.subchapters
          a(id="#{subchapter.contents.slug}").toc-h2.btn.btn-menu(href="/dist/#{subchapter.contents.url}")
            span.toc-number #{subchapter.contents.number}
            | #{subchapter.contents.title}
